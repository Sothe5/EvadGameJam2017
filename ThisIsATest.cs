using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThisIsATest : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log ("Okey, this doesn't look so bad.");
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
